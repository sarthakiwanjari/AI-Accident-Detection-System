# AccidentWatch v3 — GPU-accelerated requirements
# Install order matters — run install.bat instead of pip install directly

flask==3.0.0
flask-socketio==5.3.6
python-dotenv==1.0.0
eventlet==0.35.1
twilio==8.10.0
opencv-python==4.9.0.80
numpy==1.26.4
Pillow==10.2.0
scipy==1.12.0
deep-sort-realtime==1.3.2
requests==2.31.0
geopy==2.4.1
pandas==2.2.0

# ultralytics installed separately in install.bat
# torch installed separately (GPU version)
